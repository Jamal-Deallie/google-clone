import { Typography, Box } from '@mui/material';

export default function Header() {
  return (
    <Box>
      {/* //Goggle logo search bar flex */}
      <Typography> Header</Typography>
    </Box>
  );
}
